void setup() {
size(640, 360);
noStroke();
}
void draw() {
  background(100);
float c1 = constrain(mouseX, 0.25*width, 0.75*width);
float c2 = constrain(mouseY, 0.25*height, 0.75*height);
fill(c1);
ellipse(c1, c2, 50, 50);

}
