void setup() {
size(300, 300);
background(255);
stroke(0);
}
void draw() {
  float speed =abs(mouseX-pmouseX) + abs(mouseY-pmouseY);
  strokeWeight(speed);
  line(pmouseX, pmouseY, mouseX, mouseY );
}
